{
  "name": "PayPeriod",
  "type": "object",
  "properties": {
    "label": {
      "type": "string",
      "description": "e.g. April 2026"
    },
    "period_start": {
      "type": "string",
      "format": "date"
    },
    "period_end": {
      "type": "string",
      "format": "date"
    },
    "pay_date": {
      "type": "string",
      "format": "date"
    },
    "frequency": {
      "type": "string",
      "enum": [
        "weekly",
        "fortnightly",
        "monthly"
      ],
      "default": "monthly"
    },
    "status": {
      "type": "string",
      "enum": [
        "open",
        "closed",
        "paid"
      ],
      "default": "open"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "label",
    "period_start",
    "period_end"
  ]
}